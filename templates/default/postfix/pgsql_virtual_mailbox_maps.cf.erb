user = mailauth
password = <%= @node[:mailserver][:db_mailauth_password] %>
hosts = localhost
dbname = mailconfig
query = SELECT (maildir || 'Maildir/') AS maildir FROM mailbox WHERE username='%s' AND active = '1'

